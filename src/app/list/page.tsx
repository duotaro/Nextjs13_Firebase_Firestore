'use client';
import styles from '../page.module.css'

export default function Signup() {
  // 一覧取得

  return (
    <main className={`${styles.main} row`}>
      <div className="col">
    一覧表示
      </div>
    </main>
  )
}
